name: 'Joa Dev'
services:
  db:
    image: postgres:15
    container_name: postgres-container
    env_file:
      - .env
    ports:
      - '5432:5432'

  redis:
    image: redis:7
    container_name: redis-container
    ports:
      - '6379:6379'
